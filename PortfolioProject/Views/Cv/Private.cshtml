<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
<div class="container min-vh-100 d-flex justify-content-center align-items-center">
    <div class="border rounded p-4 d-flex flex-column justify-content-end text-center"
         style="width: 400px; height: 400px;">

        <div class="d-flex flex-grow-1 justify-content-center align-items-center">
            <i class="bi bi-lock-fill text-muted" style="font-size: 6rem;"></i>

        </div>

        <p class="mb-4">
            Denna profil är låst och visas enbart för inloggade användare.
        </p>

        <div class="row g-2">
            <div class="col-6">
                <form asp-controller="Account" asp-action="Login" method="get">
                    <button type="submit" class="btn btn-primary w-100">
                        Logga in
                    </button>
                </form>
            </div>

            <div class="col-6">
                <form asp-controller="Account" asp-action="Register" method="get">
                    <button type="submit" class="btn btn-secondary w-100">
                        Registrera
                    </button>
                </form>
            </div>
        </div>

    </div>
</div>
